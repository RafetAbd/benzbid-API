import { RequestHandler } from "express";
import  Car  from "../../db/models/Car";
import { User } from "../../db/models/User";

export const createCar: RequestHandler = (req, res, next) => {
};

export const getAllCars: RequestHandler = (req, res, next) => {
};

export const getOneCar: RequestHandler = (req, res, next) => {
};

export const updateCar: RequestHandler = (req, res, next) => {
};

export const deleteCar: RequestHandler = (req, res, next) => {
};
